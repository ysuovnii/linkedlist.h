#include "linkedlist.h"
#include <bits/stdc++.h>
using namespace std; 

int main() {
    linkedlist l;
    // l.dlt();
    // l.insertStart(10);
    // l.insertMiddle(20, 1); 
    // l.insertMiddle(25, 2);
    // l.insertEnd(30);

    // l.display();

    // l.dlt();

    // l.display(); 

    // l.Search(40);  cout << endl; 
    // l.Size();  cout << endl; 
    // l.display(); cout << endl;
    // l.insertEnd(40); 
    // l.Search(40); cout << endl; 
    // l.Size(); cout << endl;
    // l.dlt(); 
    // l.Size(); cout << endl;
    // l.display();  cout << endl; 
    

    return 0;
}
